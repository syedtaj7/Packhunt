# 🎯 PackHunt - Modern Package Discovery Platform# 🎯 PackHunt - Modern Package Discovery Platform



<div align="center">



![PackHunt Banner](https://img.shields.io/badge/PackHunt-Package_Discovery-blue?style=for-the-badge)<div align="center">## Project info

[![License](https://img.shields.io/badge/license-MIT-green?style=for-the-badge)](LICENSE)

[![TypeScript](https://img.shields.io/badge/TypeScript-007ACC?style=for-the-badge&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

[![React](https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB)](https://reactjs.org/)

[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)](https://www.postgresql.org/)![PackHunt Banner](https://img.shields.io/badge/PackHunt-Package_Discovery-blue?style=for-the-badge)



**Discover the best packages across Python, Node.js, and Rust ecosystems with AI-powered semantic search - 100% FREE!**[![License](https://img.shields.io/badge/license-MIT-green?style=for-the-badge)](LICENSE)



[🚀 Features](#-features) • [📋 Prerequisites](#-prerequisites) • [⚡ Quick Start](#-quick-start) • [🛠️ Development](#️-development) • [🔍 Search Modes](#-search-modes)[![TypeScript](https://img.shields.io/badge/TypeScript-007ACC?style=for-the-badge&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)



</div>[![React](https://img.shields.io/badge/React-20232A?style=for-the-badge&logo=react&logoColor=61DAFB)](https://reactjs.org/)



---[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)](https://www.postgresql.org/)



## 🌟 Features



<table>**Discover the best packages across Python, Node.js, and Rust ecosystems with AI-powered semantic search - 100% FREE!

<tr>

<td width="50%">



### 🤖 **AI-Powered Search**[🚀 Features](#-features) • [📋 Prerequisites](#-prerequisites) • [⚡ Quick Start](#-quick-start) • [🛠️ Development](#️-development) • [🔍 Search Modes](#-search-modes)

- **Semantic Search**: Natural language understanding using local AI

- **Hybrid Search**: Best of both worlds (60% AI + 40% keyword)

- **Keyword Search**: Lightning-fast typo-tolerant search

- **100% FREE**: No API keys, runs locally</div>



</td>

<td width="50%">

---**Use your preferred IDE**

### 🎨 **Modern Interface**

- **Dark/Light Mode**: Beautiful UI with theme switching

- **Responsive Design**: Works on desktop, tablet, and mobile

- **Real-time Filtering**: Filter by language, stars, license## 🌟 FeaturesIf you want to work locally using your own IDE, you can clone this repo and push changes. Pushed changes will also be reflected in Lovable.

- **Smart Suggestions**: Example queries to get started



</td>

</tr><table>The only requirement is having Node.js & npm installed - [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating)

<tr>

<td width="50%"><tr>



### 📦 **Rich Package Data**<td width="50%">Follow these steps:

- **30+ Curated Packages**: Python, Node.js, Rust

- **Detailed Information**: Stars, downloads, docs, install commands

- **Category Organization**: Find packages by use case

- **Star Your Favorites**: Keep track of packages you love### 🤖 **AI-Powered Search**```sh



</td>- **Semantic Search**: Natural language understanding using local AI# Step 1: Clone the repository using the project's Git URL.

<td width="50%">

- **Hybrid Search**: Best of both worlds (60% AI + 40% keyword)git clone <YOUR_GIT_URL>

### ⚡ **High Performance**

- **Fast API**: Express backend with rate limiting- **Keyword Search**: Lightning-fast typo-tolerant search

- **Vector Search**: PostgreSQL with pgvector extension

- **Caching**: React Query for optimal data fetching- **100% FREE**: No API keys, runs locally# Step 2: Navigate to the project directory.

- **Optimized**: < 200ms response times

cd <YOUR_PROJECT_NAME>

</td>

</tr></td>

</table>

<td width="50%"># Step 3: Install the necessary dependencies.

---

npm i

## 📋 Prerequisites

### 🎨 **Modern Interface**

Before you begin, ensure you have the following installed:

- **Dark/Light Mode**: Beautiful UI with theme switching# Step 4: Start the development server with auto-reloading and an instant preview.

### Required

- **Responsive Design**: Works on desktop, tablet, and mobilenpm run dev

| Software | Version | Download | Purpose |

|----------|---------|----------|---------|- **Real-time Filtering**: Filter by language, stars, license```

| **Node.js** | 18+ | [Download](https://nodejs.org/) | JavaScript runtime |

| **Bun** | Latest | [Download](https://bun.sh/) | Fast package manager & runtime |- **Smart Suggestions**: Example queries to get started

| **PostgreSQL** | 14+ | [Download](https://www.postgresql.org/download/) | Database (or use Supabase) |

| **Git** | Latest | [Download](https://git-scm.com/) | Version control |**Edit a file directly in GitHub**



### Optional (Recommended)</td>



| Software | Version | Download | Purpose |</tr>- Navigate to the desired file(s).

|----------|---------|----------|---------|

| **Meilisearch** | 1.10+ | [Download](https://github.com/meilisearch/meilisearch/releases) | Keyword search engine |<tr>- Click the "Edit" button (pencil icon) at the top right of the file view.

| **VS Code** | Latest | [Download](https://code.visualstudio.com/) | Code editor |

<td width="50%">- Make your changes and commit the changes.

### PostgreSQL Extensions Required



```sql

-- Enable pgvector for semantic search### 📦 **Rich Package Data****Use GitHub Codespaces**

CREATE EXTENSION IF NOT EXISTS vector;

```- **30+ Curated Packages**: Python, Node.js, Rust



> 💡 **Pro Tip**: Use [Supabase](https://supabase.com/) for a free PostgreSQL database with pgvector already enabled!- **Detailed Information**: Stars, downloads, docs, install commands- Navigate to the main page of your repository.



---- **Category Organization**: Find packages by use case- Click on the "Code" button (green button) near the top right.



## ⚡ Quick Start- **Star Your Favorites**: Keep track of packages you love- Select the "Codespaces" tab.



### 1️⃣ Clone the Repository- Click on "New codespace" to launch a new Codespace environment.



```bash</td>- Edit files directly within the Codespace and commit and push your changes once you're done.

git clone https://github.com/syedtaj7/Packhunt.git

cd Packhunt<td width="50%">

```

## What technologies are used for this project?

### 2️⃣ Install Dependencies

### ⚡ **High Performance**

```bash

# Install frontend dependencies- **Fast API**: Express backend with rate limitingThis project is built with:

bun install

- **Vector Search**: PostgreSQL with pgvector extension

# Install backend dependencies

cd server- **Caching**: React Query for optimal data fetching- Vite

bun install

cd ..- **Optimized**: < 200ms response times- TypeScript

```

- React

### 3️⃣ Set Up Environment Variables

</td>- shadcn-ui

#### Backend Environment (`.env` in `server/` directory)

</tr>- Tailwind CSS

```bash

# Navigate to server directory</table>

cd server

## How can I deploy this project?

# Copy example environment file

cp .env.example .env---



# Edit .env file with your valuesSimply open [Lovable](https://lovable.dev/projects/REPLACE_WITH_PROJECT_ID) and click on Share -> Publish.

```

## 📋 Prerequisites

**Required Environment Variables:**

## Can I connect a custom domain to my Lovable project?

```env

# Database Configuration (Supabase or Local PostgreSQL)Before you begin, ensure you have the following installed:

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true"

DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"Yes, you can!



# Meilisearch Configuration### Required

MEILISEARCH_HOST="http://127.0.0.1:7700"

MEILISEARCH_API_KEY="your-master-key-here"To connect a domain, navigate to Project > Settings > Domains and click Connect Domain.



# Server Configuration| Software | Version | Download | Purpose |

PORT=3001

NODE_ENV=development|----------|---------|----------|---------|Read more here: [Setting up a custom domain](https://docs.lovable.dev/features/custom-domain#custom-domain)



# Frontend URL (for CORS)| **Node.js** | 18+ | [Download](https://nodejs.org/) | JavaScript runtime |

FRONTEND_URL="http://localhost:8080"| **Bun** | Latest | [Download](https://bun.sh/) | Fast package manager & runtime |

```| **PostgreSQL** | 14+ | [Download](https://www.postgresql.org/download/) | Database (or use Supabase) |

| **Git** | Latest | [Download](https://git-scm.com/) | Version control |

> 🔑 **Get Supabase Database URL**:

> 1. Create account at [supabase.com](https://supabase.com)### Optional (Recommended)

> 2. Create a new project

> 3. Go to Project Settings → Database| Software | Version | Download | Purpose |

> 4. Copy the connection strings (both pooler and direct)|----------|---------|----------|---------|

> 5. Enable pgvector extension in SQL Editor: `CREATE EXTENSION vector;`| **Meilisearch** | 1.10+ | [Download](https://github.com/meilisearch/meilisearch/releases) | Keyword search engine |

| **VS Code** | Latest | [Download](https://code.visualstudio.com/) | Code editor |

### 4️⃣ Set Up Meilisearch (Keyword Search)

### PostgreSQL Extensions Required

Meilisearch powers the fast, typo-tolerant keyword search.

```sql

#### Windows (PowerShell):-- Enable pgvector for semantic search

CREATE EXTENSION IF NOT EXISTS vector;

```powershell```

# Automated setup (recommended)

.\setup-meilisearch.ps1> 💡 **Pro Tip**: Use [Supabase](https://supabase.com/) for a free PostgreSQL database with pgvector already enabled!



# Or manual download:---

# 1. Download from https://github.com/meilisearch/meilisearch/releases

# 2. Place meilisearch.exe in meilisearch/ folder## ⚡ Quick Start

# 3. Run: .\start-meilisearch.ps1

```### 1️⃣ Clone the Repository



#### macOS/Linux:```bash

git clone https://github.com/syedtaj7/Packhunt.git

```bashcd Packhunt

# macOS```

curl -L https://install.meilisearch.com | sh

mv meilisearch meilisearch/### 2️⃣ Install Dependencies



# Linux```bash

curl -L https://install.meilisearch.com | sh# Install frontend dependencies

mv meilisearch meilisearch/bun install



# Start Meilisearch# Install backend dependencies

cd meilisearchcd server

./meilisearch --http-addr 127.0.0.1:7700 --master-key "your-master-key-here"bun install

```cd ..

```

**Verify Meilisearch is running:**

```bash### 3️⃣ Set Up Environment Variables

curl http://localhost:7700/health

# Should return: {"status":"available"}#### Backend Environment (`.env` in `server/` directory)

```

```bash

### 5️⃣ Initialize Database# Navigate to server directory

cd server

```bash

cd server# Copy example environment file

cp .env.example .env

# Generate Prisma Client

bun prisma generate# Edit .env file with your values

```

# Run database migrations

bun prisma migrate dev**Required Environment Variables:**



# Seed the database with 30 curated packages```env

bun prisma db seed# Database Configuration (Supabase or Local PostgreSQL)

```DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true"

DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

**Expected Output:**

```# Meilisearch Configuration

✅ Seeded 30 packagesMEILISEARCH_HOST="http://127.0.0.1:7700"

✅ Seeded 8 categoriesMEILISEARCH_API_KEY="your-master-key-here"

✅ Created category relationships

🎉 Database seeded successfully!# Server Configuration

```PORT=3001

NODE_ENV=development

### 6️⃣ Generate AI Embeddings (Semantic Search)

# Frontend URL (for CORS)

This step generates vector embeddings for semantic search using a local AI model (no API keys needed!).FRONTEND_URL="http://localhost:8080"

```

```bash

cd server> 🔑 **Get Supabase Database URL**:

> 1. Create account at [supabase.com](https://supabase.com)

# Generate embeddings for all packages (~30 seconds)> 2. Create a new project

bun run generate-embeddings> 3. Go to Project Settings → Database

```> 4. Copy the connection strings (both pooler and direct)

> 5. Enable pgvector extension in SQL Editor: `CREATE EXTENSION vector;`

**Expected Output:**

```### 4️⃣ Set Up Meilisearch (Keyword Search)

🤖 Starting embedding generation...

📦 Found 30 packages without embeddingsMeilisearch powers the fast, typo-tolerant keyword search.

✅ Generated embeddings for: numpy

✅ Generated embeddings for: pandas#### Windows (PowerShell):

...

🎉 Successfully generated embeddings for 30/30 packages```powershell

```# Automated setup (recommended)

.\setup-meilisearch.ps1

> 💡 **First Run**: The AI model (~23MB) will download automatically. Subsequent runs are instant!

# Or manual download:

### 7️⃣ Sync Data to Meilisearch# 1. Download from https://github.com/meilisearch/meilisearch/releases

# 2. Place meilisearch.exe in meilisearch/ folder

```bash# 3. Run: .\start-meilisearch.ps1

cd server```



# Sync all packages to Meilisearch#### macOS/Linux:

bun run sync-meilisearch

``````bash

# macOS

**Expected Output:**curl -L https://install.meilisearch.com | sh

```mv meilisearch meilisearch/

🔄 Starting Meilisearch sync...

✅ Connected to Meilisearch# Linux

📦 Found 30 packages in databasecurl -L https://install.meilisearch.com | sh

✅ Indexed 30 packagesmv meilisearch meilisearch/

⚙️ Updated searchable attributes

⚙️ Updated filterable attributes# Start Meilisearch

⚙️ Updated sortable attributescd meilisearch

🎉 Sync completed successfully!./meilisearch --http-addr 127.0.0.1:7700 --master-key "your-master-key-here"

``````



---**Verify Meilisearch is running:**

```bash

## 🚀 Running the Applicationcurl http://localhost:7700/health

# Should return: {"status":"available"}

You need to run **THREE** processes simultaneously. Open three terminal windows:```



### Terminal 1: Backend API Server 🔧### 5️⃣ Initialize Database



```bash```bash

cd servercd server

bun run dev

```# Generate Prisma Client

bun prisma generate

**Expected Output:**

```# Run database migrations

🚀 PackHunt API Server is running!bun prisma migrate dev



📍 Local:    http://localhost:3001# Seed the database with 30 curated packages

🔗 Health:   http://localhost:3001/healthbun prisma db seed

📦 Packages: http://localhost:3001/api/packages```

🔍 Search:   http://localhost:3001/api/search

📂 Categories: http://localhost:3001/api/categories**Expected Output:**

```

Environment: development✅ Seeded 30 packages

Frontend: http://localhost:8080✅ Seeded 8 categories

```✅ Created category relationships

🎉 Database seeded successfully!

**✅ Verify**: Open http://localhost:3001/health in browser (should show `{"status":"ok"}`)```



### Terminal 2: Meilisearch Server 🔍### 6️⃣ Generate AI Embeddings (Semantic Search)



```bashThis step generates vector embeddings for semantic search using a local AI model (no API keys needed!).

# Windows

.\start-meilisearch.ps1```bash

cd server

# macOS/Linux

cd meilisearch# Generate embeddings for all packages (~30 seconds)

./meilisearch --http-addr 127.0.0.1:7700 --master-key "your-master-key-here"bun run generate-embeddings

``````



**Expected Output:****Expected Output:**

``````

Meilisearch v1.10.3🤖 Starting embedding generation...

📦 Found 30 packages without embeddings

Server is listening on: http://127.0.0.1:7700✅ Generated embeddings for: numpy

```✅ Generated embeddings for: pandas

...

**✅ Verify**: Open http://localhost:7700/health in browser (should show `{"status":"available"}`)🎉 Successfully generated embeddings for 30/30 packages

```

### Terminal 3: Frontend Development Server 🎨

> 💡 **First Run**: The AI model (~23MB) will download automatically. Subsequent runs are instant!

```bash

bun run dev### 7️⃣ Sync Data to Meilisearch

```

```bash

**Expected Output:**cd server

```

  VITE v5.4.21  ready in 324 ms# Sync all packages to Meilisearch

bun run sync-meilisearch

  ➜  Local:   http://localhost:8080/```

  ➜  Network: use --host to expose

  ➜  press h + enter to show help**Expected Output:**

``````

🔄 Starting Meilisearch sync...

**✅ Verify**: Open http://localhost:8080 in browser✅ Connected to Meilisearch

📦 Found 30 packages in database

---✅ Indexed 30 packages

⚙️ Updated searchable attributes

## 🎉 Access the Application⚙️ Updated filterable attributes

⚙️ Updated sortable attributes

### 🌐 Open http://localhost:8080🎉 Sync completed successfully!

```

You should see:

- ✅ Hero section with search bar---

- ✅ Featured packages

- ✅ Language selection (Python, Node.js, Rust)## 🚀 Running the Application

- ✅ Fully functional search

You need to run **THREE** processes simultaneously. Open three terminal windows:

### Quick Test

### Terminal 1: Backend API Server 🔧

1. **Search for "math"** - Should find Matplotlib, NumPy

2. **Click "Smart Search"** - Try natural language: "plotting library"```bash

3. **Click "AI Only"** - Try: "data manipulation tool"cd server

4. **Filter by Python** - See only Python packagesbun run dev

5. **Star a package** - Click star icon to save favorites```



---**Expected Output:**

```

## 🔍 Search Modes🚀 PackHunt API Server is running!



PackHunt offers three powerful search modes:📍 Local:    http://localhost:3001

🔗 Health:   http://localhost:3001/health

### 1. 🔤 **Keyword Search** (Meilisearch)📦 Packages: http://localhost:3001/api/packages

- **Best for**: Exact matches, package names, known terms🔍 Search:   http://localhost:3001/api/search

- **Features**: Typo tolerance, instant results📂 Categories: http://localhost:3001/api/categories

- **Example**: "mathplotlib" → finds "Matplotlib"

- **Speed**: ~30-60msEnvironment: development

Frontend: http://localhost:8080

### 2. 🧠 **AI Only** (Semantic Search)```

- **Best for**: Natural language queries, concepts

- **Features**: Understands intent, related packages**✅ Verify**: Open http://localhost:3001/health in browser (should show `{"status":"ok"}`)

- **Example**: "plotting library for data visualization"

- **Speed**: ~80-120ms### Terminal 2: Meilisearch Server 🔍



### 3. ⚡ **Smart Search** (Hybrid - RECOMMENDED)```bash

- **Best for**: Everything! Best of both worlds# Windows

- **Features**: 60% AI + 40% Keyword.\start-meilisearch.ps1

- **Example**: "fast web framework for APIs"

- **Speed**: ~100-200ms# macOS/Linux

cd meilisearch

**Try These Example Queries:**./meilisearch --http-addr 127.0.0.1:7700 --master-key "your-master-key-here"

``````

✨ fast web framework for APIs

✨ plotting library for data visualization**Expected Output:**

✨ async HTTP client```

✨ ORM for databaseMeilisearch v1.10.3

✨ parallel computing library

```Server is listening on: http://127.0.0.1:7700

```

---

**✅ Verify**: Open http://localhost:7700/health in browser (should show `{"status":"available"}`)

## 🛠️ Development

### Terminal 3: Frontend Development Server 🎨

### Project Structure

```bash

```bun run dev

PackHunt/```

├── 📁 src/                      # Frontend React app

│   ├── 📁 components/           # Reusable UI components**Expected Output:**

│   │   ├── Header.tsx           # Navigation header```

│   │   ├── Footer.tsx           # Page footer  VITE v5.4.21  ready in 324 ms

│   │   ├── SearchBar.tsx        # Search input with clear

│   │   ├── PackageCard.tsx      # Package display card  ➜  Local:   http://localhost:8080/

│   │   ├── FilterBar.tsx        # Filter controls  ➜  Network: use --host to expose

│   │   └── 📁 ui/               # Shadcn UI components  ➜  press h + enter to show help

│   ├── 📁 pages/                # Route pages```

│   │   ├── Index.tsx            # Homepage

│   │   ├── SearchPage.tsx       # Search results**✅ Verify**: Open http://localhost:8080 in browser

│   │   ├── PackageDetail.tsx    # Package details

│   │   ├── LanguagesPage.tsx    # Language grid---

│   │   └── StarredPage.tsx      # Starred packages

│   ├── 📁 hooks/                # Custom React hooks## 🎉 Access the Application

│   │   └── useApi.ts            # API data fetching

│   ├── 📁 lib/                  # Utility functions### 🌐 Open http://localhost:8080

│   │   ├── api.ts               # API client

│   │   └── utils.ts             # Helper functionsYou should see:

│   └── App.tsx                  # Main app component- ✅ Hero section with search bar

│- ✅ Featured packages

├── 📁 server/                   # Backend Express API- ✅ Language selection (Python, Node.js, Rust)

│   ├── 📁 src/- ✅ Fully functional search

│   │   ├── index.ts             # Server entry point

│   │   ├── 📁 routes/           # API routes### Quick Test

│   │   │   ├── packages.ts      # Package CRUD endpoints

│   │   │   ├── search.ts        # Search endpoints1. **Search for "math"** - Should find Matplotlib, NumPy

│   │   │   └── categories.ts    # Category endpoints2. **Click "Smart Search"** - Try natural language: "plotting library"

│   │   ├── 📁 services/         # Business logic3. **Click "AI Only"** - Try: "data manipulation tool"

│   │   │   ├── embeddings.ts    # AI embedding generation4. **Filter by Python** - See only Python packages

│   │   │   ├── semantic-search.ts  # Vector search5. **Star a package** - Click star icon to save favorites

│   │   │   └── meilisearch.ts   # Keyword search

│   │   ├── 📁 scripts/          # Utility scripts---

│   │   │   ├── generate-embeddings.ts

│   │   │   └── sync-meilisearch.ts## 🔍 Search Modes

│   │   └── 📁 lib/

│   │       └── prisma.ts        # Database clientPackHunt offers three powerful search modes:

│   ├── 📁 prisma/

│   │   ├── schema.prisma        # Database schema### 1. 🔤 **Keyword Search** (Meilisearch)

│   │   ├── seed.ts              # Seed data (30 packages)- **Best for**: Exact matches, package names, known terms

│   │   └── 📁 migrations/       # Database migrations- **Features**: Typo tolerance, instant results

│   ├── .env.example             # Environment template- **Example**: "mathplotlib" → finds "Matplotlib"

│   └── package.json- **Speed**: ~30-60ms

│

├── 📁 meilisearch/              # Meilisearch data### 2. 🧠 **AI Only** (Semantic Search)

│   ├── meilisearch.exe          # (Download separately)- **Best for**: Natural language queries, concepts

│   └── 📁 data/                 # Search index data (gitignored)- **Features**: Understands intent, related packages

│- **Example**: "plotting library for data visualization"

├── setup-meilisearch.ps1        # Meilisearch setup script- **Speed**: ~80-120ms

├── start-meilisearch.ps1        # Start Meilisearch

├── MEILISEARCH-SETUP.md         # Meilisearch docs### 3. ⚡ **Smart Search** (Hybrid - RECOMMENDED)

├── package.json- **Best for**: Everything! Best of both worlds

└── README.md- **Features**: 60% AI + 40% Keyword

```- **Example**: "fast web framework for APIs"

- **Speed**: ~100-200ms

### Available Scripts

**Try These Example Queries:**

#### Frontend Scripts```

✨ fast web framework for APIs

```bash✨ plotting library for data visualization

bun run dev          # Start development server (localhost:8080)✨ async HTTP client

bun run build        # Build for production✨ ORM for database

bun run preview      # Preview production build✨ parallel computing library

bun run lint         # Run ESLint```

```

---

#### Backend Scripts

## 🛠️ Development

```bash

cd server### Project Structure



# Development```

bun run dev                    # Start dev server with nodemon (localhost:3001)PackHunt/

bun run build                  # Build TypeScript to JavaScript├── 📁 src/                      # Frontend React app

bun run start                  # Start production server│   ├── 📁 components/           # Reusable UI components

│   │   ├── Header.tsx           # Navigation header

# Database Management│   │   ├── Footer.tsx           # Page footer

bun prisma generate            # Generate Prisma Client│   │   ├── SearchBar.tsx        # Search input with clear

bun prisma migrate dev         # Create & run new migration│   │   ├── PackageCard.tsx      # Package display card

bun prisma migrate deploy      # Deploy migrations (production)│   │   ├── FilterBar.tsx        # Filter controls

bun prisma db seed             # Seed database with packages│   │   └── 📁 ui/               # Shadcn UI components

bun prisma studio              # Open Prisma Studio GUI│   ├── 📁 pages/                # Route pages

bun prisma db push             # Push schema without migration│   │   ├── Index.tsx            # Homepage

│   │   ├── SearchPage.tsx       # Search results

# Data Utilities│   │   ├── PackageDetail.tsx    # Package details

bun run generate-embeddings    # Generate AI embeddings for all packages│   │   ├── LanguagesPage.tsx    # Language grid

bun run sync-meilisearch       # Sync packages to Meilisearch index│   │   └── StarredPage.tsx      # Starred packages

```│   ├── 📁 hooks/                # Custom React hooks

│   │   └── useApi.ts            # API data fetching

### API Endpoints│   ├── 📁 lib/                  # Utility functions

│   │   ├── api.ts               # API client

#### Health Check│   │   └── utils.ts             # Helper functions

│   └── App.tsx                  # Main app component

```http│

GET /health                    # Server health check├── 📁 server/                   # Backend Express API

```│   ├── 📁 src/

│   │   ├── index.ts             # Server entry point

#### Packages│   │   ├── 📁 routes/           # API routes

│   │   │   ├── packages.ts      # Package CRUD endpoints

```http│   │   │   ├── search.ts        # Search endpoints

GET /api/packages              # List all packages│   │   │   └── categories.ts    # Category endpoints

  ?language=PYTHON             # Filter by language│   │   ├── 📁 services/         # Business logic

  &minStars=1000               # Minimum stars│   │   │   ├── embeddings.ts    # AI embedding generation

  &license=MIT                 # Filter by license│   │   │   ├── semantic-search.ts  # Vector search

  &sortBy=stars                # Sort by: stars, downloads, recent, name│   │   │   └── meilisearch.ts   # Keyword search

  &limit=50                    # Results per page│   │   ├── 📁 scripts/          # Utility scripts

  &page=1                      # Page number│   │   │   ├── generate-embeddings.ts

│   │   │   └── sync-meilisearch.ts

GET /api/packages/:slug        # Get package by slug│   │   └── 📁 lib/

GET /api/packages/:slug/stats  # Get package statistics│   │       └── prisma.ts        # Database client

GET /api/packages/trending     # Get trending packages│   ├── 📁 prisma/

```│   │   ├── schema.prisma        # Database schema

│   │   ├── seed.ts              # Seed data (30 packages)

#### Search│   │   └── 📁 migrations/       # Database migrations

│   ├── .env.example             # Environment template

```http│   └── package.json

GET /api/search/meilisearch    # Keyword search (typo-tolerant)│

GET /api/search/semantic       # AI semantic search├── 📁 meilisearch/              # Meilisearch data

GET /api/search/hybrid         # Hybrid search (RECOMMENDED)│   ├── meilisearch.exe          # (Download separately)

│   └── 📁 data/                 # Search index data (gitignored)

Query Parameters:│

  ?q=query                     # Search query (required)├── setup-meilisearch.ps1        # Meilisearch setup script

  &language=PYTHON             # Filter by language├── start-meilisearch.ps1        # Start Meilisearch

  &minStars=1000               # Minimum stars├── MEILISEARCH-SETUP.md         # Meilisearch docs

  &license=MIT                 # Filter by license├── package.json

  &sortBy=stars                # Sort by: relevance, stars, downloads, recent, name└── README.md

  &limit=20                    # Results per page (default: 20)```

  &page=1                      # Page number (default: 1)

```### Available Scripts



**Example Requests:**#### Frontend Scripts



```bash```bash

# Keyword searchbun run dev          # Start development server (localhost:8080)

curl "http://localhost:3001/api/search/meilisearch?q=math"bun run build        # Build for production

bun run preview      # Preview production build

# Semantic search (natural language)bun run lint         # Run ESLint

curl "http://localhost:3001/api/search/semantic?q=plotting+library"```



# Hybrid search with filters#### Backend Scripts

curl "http://localhost:3001/api/search/hybrid?q=web+framework&language=PYTHON&minStars=10000"

``````bash

cd server

#### Categories

# Development

```httpbun run dev                    # Start dev server with nodemon (localhost:3001)

GET /api/categories            # List all categoriesbun run build                  # Build TypeScript to JavaScript

GET /api/categories/:name/packages  # Get packages in categorybun run start                  # Start production server

```

# Database Management

### Environment Variables Referencebun prisma generate            # Generate Prisma Client

bun prisma migrate dev         # Create & run new migration

#### Backend (`server/.env`)bun prisma migrate deploy      # Deploy migrations (production)

bun prisma db seed             # Seed database with packages

| Variable | Description | Example | Required |bun prisma studio              # Open Prisma Studio GUI

|----------|-------------|---------|----------|bun prisma db push             # Push schema without migration

| `DATABASE_URL` | PostgreSQL connection (pooler) | `postgresql://...?pgbouncer=true` | ✅ Yes |

| `DIRECT_URL` | PostgreSQL direct connection | `postgresql://...` | ✅ Yes |# Data Utilities

| `MEILISEARCH_HOST` | Meilisearch server URL | `http://127.0.0.1:7700` | ✅ Yes |bun run generate-embeddings    # Generate AI embeddings for all packages

| `MEILISEARCH_API_KEY` | Meilisearch master key | `your-master-key-here` | ✅ Yes |bun run sync-meilisearch       # Sync packages to Meilisearch index

| `PORT` | Backend server port | `3001` | ❌ No (default: 3001) |```

| `NODE_ENV` | Environment mode | `development` or `production` | ❌ No |

| `FRONTEND_URL` | Frontend URL for CORS | `http://localhost:8080` | ❌ No |### API Endpoints



---#### Health Check



## 🐛 Troubleshooting```http

GET /health                    # Server health check

### Common Issues & Solutions```



#### ❌ Problem: "Failed to fetch" Error#### Packages



**Symptoms**: Frontend shows error alert, can't load packages```http

GET /api/packages              # List all packages

**Solution**:  ?language=PYTHON             # Filter by language

```bash  &minStars=1000               # Minimum stars

# 1. Check if backend is running  &license=MIT                 # Filter by license

curl http://localhost:3001/health  &sortBy=stars                # Sort by: stars, downloads, recent, name

# Should return: {"status":"ok"}  &limit=50                    # Results per page

  &page=1                      # Page number

# 2. If not running, start backend

cd serverGET /api/packages/:slug        # Get package by slug

bun run devGET /api/packages/:slug/stats  # Get package statistics

GET /api/packages/trending     # Get trending packages

# 3. Check for port conflicts```

netstat -ano | findstr :3001

#### Search

# 4. Verify CORS settings in server/src/index.ts

``````http

GET /api/search/meilisearch    # Keyword search (typo-tolerant)

#### ❌ Problem: "Meilisearch connection failed"GET /api/search/semantic       # AI semantic search

GET /api/search/hybrid         # Hybrid search (RECOMMENDED)

**Symptoms**: Keyword search doesn't work, 500 errors

Query Parameters:

**Solution**:  ?q=query                     # Search query (required)

```bash  &language=PYTHON             # Filter by language

# 1. Check if Meilisearch is running  &minStars=1000               # Minimum stars

curl http://localhost:7700/health  &license=MIT                 # Filter by license

  &sortBy=stars                # Sort by: relevance, stars, downloads, recent, name

# 2. Start Meilisearch  &limit=20                    # Results per page (default: 20)

.\start-meilisearch.ps1  # Windows  &page=1                      # Page number (default: 1)

cd meilisearch && ./meilisearch --http-addr 127.0.0.1:7700  # macOS/Linux```



# 3. Re-sync data**Example Requests:**

cd server

bun run sync-meilisearch```bash

```# Keyword search

curl "http://localhost:3001/api/search/meilisearch?q=math"

#### ❌ Problem: "Database connection error"

# Semantic search (natural language)

**Symptoms**: `P1001: Can't reach database server`, backend won't startcurl "http://localhost:3001/api/search/semantic?q=plotting+library"



**Solution**:# Hybrid search with filters

```bashcurl "http://localhost:3001/api/search/hybrid?q=web+framework&language=PYTHON&minStars=10000"

# 1. Check .env file exists```

cd server

cat .env  # Should show DATABASE_URL and DIRECT_URL#### Categories



# 2. Test connection```http

bun prisma db pullGET /api/categories            # List all categories

GET /api/categories/:name/packages  # Get packages in category

# 3. Verify pgvector extension```

# In Supabase SQL Editor:

CREATE EXTENSION IF NOT EXISTS vector;### Environment Variables Reference



# 4. Check firewall/network#### Backend (`server/.env`)

ping your-database-host

```| Variable | Description | Example | Required |

|----------|-------------|---------|----------|

#### ❌ Problem: "No packages found"| `DATABASE_URL` | PostgreSQL connection (pooler) | `postgresql://...?pgbouncer=true` | ✅ Yes |

| `DIRECT_URL` | PostgreSQL direct connection | `postgresql://...` | ✅ Yes |

**Symptoms**: Empty search results, no packages on homepage| `MEILISEARCH_HOST` | Meilisearch server URL | `http://127.0.0.1:7700` | ✅ Yes |

| `MEILISEARCH_API_KEY` | Meilisearch master key | `your-master-key-here` | ✅ Yes |

**Solution**:| `PORT` | Backend server port | `3001` | ❌ No (default: 3001) |

```bash| `NODE_ENV` | Environment mode | `development` or `production` | ❌ No |

# 1. Seed database| `FRONTEND_URL` | Frontend URL for CORS | `http://localhost:8080` | ❌ No |

cd server

bun prisma db seed---



# 2. Generate embeddings## 🐛 Troubleshooting

bun run generate-embeddings

### Common Issues & Solutions

# 3. Sync to Meilisearch

bun run sync-meilisearch#### ❌ Problem: "Failed to fetch" Error



# 4. Verify data**Symptoms**: Frontend shows error alert, can't load packages

bun prisma studio

# Check if packages exist in database**Solution**:

``````bash

# 1. Check if backend is running

#### ❌ Problem: Port Already in Usecurl http://localhost:3001/health

# Should return: {"status":"ok"}

**Symptoms**: `EADDRINUSE: address already in use :::3001` or `:8080`

# 2. If not running, start backend

**Solution**:cd server

```bashbun run dev

# Windows - Kill process on port

netstat -ano | findstr :3001# 3. Check for port conflicts

taskkill /PID <PID_NUMBER> /Fnetstat -ano | findstr :3001



# macOS/Linux - Kill process on port# 4. Verify CORS settings in server/src/index.ts

lsof -ti:3001 | xargs kill -9```



# Or change port in config#### ❌ Problem: "Meilisearch connection failed"

# Backend: server/.env → PORT=3002

# Frontend: vite.config.ts → server: { port: 8081 }**Symptoms**: Keyword search doesn't work, 500 errors

```

**Solution**:

#### ❌ Problem: "Cannot read properties of undefined (reading 'toString')"```bash

# 1. Check if Meilisearch is running

**Symptoms**: React error, blank screen, console errorscurl http://localhost:7700/health



**Solution**: Already fixed! Update your code:# 2. Start Meilisearch

```bash.\start-meilisearch.ps1  # Windows

git pull origin maincd meilisearch && ./meilisearch --http-addr 127.0.0.1:7700  # macOS/Linux

```

# 3. Re-sync data

The `formatNumber` function in `PackageCard.tsx` now handles undefined values.cd server

bun run sync-meilisearch

#### ❌ Problem: Embeddings generation is slow```



**Symptoms**: `generate-embeddings` takes too long#### ❌ Problem: "Database connection error"



**Solution**:**Symptoms**: `P1001: Can't reach database server`, backend won't start

```bash

# This is normal for first run!**Solution**:

# The AI model (~23MB) downloads automatically```bash

# Subsequent runs are much faster# 1. Check .env file exists

cd server

# If stuck, check:cat .env  # Should show DATABASE_URL and DIRECT_URL

# 1. Internet connection (for model download)

# 2. Disk space (need ~50MB)# 2. Test connection

# 3. Kill and restartbun prisma db pull

```

# 3. Verify pgvector extension

#### ❌ Problem: Search results are empty# In Supabase SQL Editor:

CREATE EXTENSION IF NOT EXISTS vector;

**Symptoms**: Search works but returns no results

# 4. Check firewall/network

**Solution**:ping your-database-host

```bash```

# 1. Check search mode

# - Keyword: Requires Meilisearch running#### ❌ Problem: "No packages found"

# - Semantic: Requires embeddings generated

# - Hybrid: Requires both**Symptoms**: Empty search results, no packages on homepage



# 2. Verify embeddings exist**Solution**:

cd server```bash

bun prisma studio# 1. Seed database

# Check Package table → embeddingGeneratedAt should have datescd server

bun prisma db seed

# 3. Check Meilisearch index

curl "http://localhost:7700/indexes/packages/stats"# 2. Generate embeddings

# Should show numberOfDocuments > 0bun run generate-embeddings

```

# 3. Sync to Meilisearch

---bun run sync-meilisearch



## 📚 Technology Stack# 4. Verify data

bun prisma studio

### Frontend# Check if packages exist in database

```

- **Framework**: [React 18](https://reactjs.org/) with TypeScript

- **Build Tool**: [Vite 5](https://vitejs.dev/) - Ultra-fast HMR#### ❌ Problem: Port Already in Use

- **Styling**: [Tailwind CSS](https://tailwindcss.com/) - Utility-first CSS

- **UI Components**: [Shadcn UI](https://ui.shadcn.com/) - Beautiful components**Symptoms**: `EADDRINUSE: address already in use :::3001` or `:8080`

- **Routing**: [React Router v6](https://reactrouter.com/) - Client-side routing

- **Data Fetching**: [TanStack Query](https://tanstack.com/query) - Async state management**Solution**:

- **Icons**: [Lucide React](https://lucide.dev/) - Beautiful icons```bash

- **State Management**: React Context + Local Storage# Windows - Kill process on port

netstat -ano | findstr :3001

### Backendtaskkill /PID <PID_NUMBER> /F



- **Runtime**: [Bun](https://bun.sh/) / Node.js - Fast JavaScript runtime# macOS/Linux - Kill process on port

- **Framework**: [Express](https://expressjs.com/) - Web frameworklsof -ti:3001 | xargs kill -9

- **Language**: TypeScript - Type-safe JavaScript

- **ORM**: [Prisma](https://www.prisma.io/) - Next-gen database toolkit# Or change port in config

- **Database**: PostgreSQL 14+ - Relational database# Backend: server/.env → PORT=3002

- **Vector Extension**: [pgvector](https://github.com/pgvector/pgvector) - Vector similarity search# Frontend: vite.config.ts → server: { port: 8081 }

- **Search Engine**: [Meilisearch](https://www.meilisearch.com/) - Fast search API```

- **AI/ML**: [Transformers.js](https://huggingface.co/docs/transformers.js) - Run transformers in Node

- **Embedding Model**: `Xenova/all-MiniLM-L6-v2` (384 dimensions)#### ❌ Problem: "Cannot read properties of undefined (reading 'toString')"



### Infrastructure & Tools**Symptoms**: React error, blank screen, console errors



- **Package Manager**: Bun - All-in-one toolkit**Solution**: Already fixed! Update your code:

- **Development**: Nodemon - Auto-reload on changes```bash

- **API Testing**: cURL, Postman compatiblegit pull origin main

- **Database GUI**: Prisma Studio - Built-in database browser```

- **Rate Limiting**: express-rate-limit (1000 req/15min)

- **CORS**: Configured for localhost developmentThe `formatNumber` function in `PackageCard.tsx` now handles undefined values.

- **Caching**: React Query (2-minute stale time)

#### ❌ Problem: Embeddings generation is slow

### Search Technology

**Symptoms**: `generate-embeddings` takes too long

- **Keyword Search**: Meilisearch with typo tolerance

- **Semantic Search**: Cosine similarity with pgvector**Solution**:

- **Hybrid Search**: 60% semantic + 40% keyword (weighted combination)```bash

- **Vector Dimensions**: 384 (MiniLM-L6-v2 model)# This is normal for first run!

- **Similarity Metric**: Cosine distance (`<=>` operator)# The AI model (~23MB) downloads automatically

# Subsequent runs are much faster

---

# If stuck, check:

## 🚀 Deployment# 1. Internet connection (for model download)

# 2. Disk space (need ~50MB)

### Prerequisites for Production# 3. Kill and restart

```

- PostgreSQL database with pgvector extension

- Meilisearch Cloud or self-hosted instance#### ❌ Problem: Search results are empty

- Node.js 18+ environment

**Symptoms**: Search works but returns no results

### Deploy Backend API

**Solution**:

#### Option 1: Railway (Recommended)```bash

# 1. Check search mode

1. Create account at [railway.app](https://railway.app/)# - Keyword: Requires Meilisearch running

2. Click "New Project" → "Deploy from GitHub"# - Semantic: Requires embeddings generated

3. Select your repository# - Hybrid: Requires both

4. Configure:

   ```# 2. Verify embeddings exist

   Root Directory: servercd server

   Build Command: bun install && bun prisma generatebun prisma studio

   Start Command: bun run start# Check Package table → embeddingGeneratedAt should have dates

   ```

5. Add environment variables (all from `.env`)# 3. Check Meilisearch index

6. Deploy!curl "http://localhost:7700/indexes/packages/stats"

# Should show numberOfDocuments > 0

#### Option 2: Render```



1. Create account at [render.com](https://render.com/)---

2. Click "New" → "Web Service"

3. Connect GitHub repository## 📚 Technology Stack

4. Configure:

   ```### Frontend

   Root Directory: server

   Build Command: bun install && bun prisma generate && bun prisma migrate deploy- **Framework**: [React 18](https://reactjs.org/) with TypeScript

   Start Command: bun run start- **Build Tool**: [Vite 5](https://vitejs.dev/) - Ultra-fast HMR

   ```- **Styling**: [Tailwind CSS](https://tailwindcss.com/) - Utility-first CSS

5. Add environment variables- **UI Components**: [Shadcn UI](https://ui.shadcn.com/) - Beautiful components

6. Deploy!- **Routing**: [React Router v6](https://reactrouter.com/) - Client-side routing

- **Data Fetching**: [TanStack Query](https://tanstack.com/query) - Async state management

#### Post-Deployment Steps- **Icons**: [Lucide React](https://lucide.dev/) - Beautiful icons

- **State Management**: React Context + Local Storage

```bash

# 1. Run migrations### Backend

railway run bun prisma migrate deploy  # Railway

# or- **Runtime**: [Bun](https://bun.sh/) / Node.js - Fast JavaScript runtime

render run bun prisma migrate deploy   # Render- **Framework**: [Express](https://expressjs.com/) - Web framework

- **Language**: TypeScript - Type-safe JavaScript

# 2. Seed database- **ORM**: [Prisma](https://www.prisma.io/) - Next-gen database toolkit

railway run bun prisma db seed- **Database**: PostgreSQL 14+ - Relational database

- **Vector Extension**: [pgvector](https://github.com/pgvector/pgvector) - Vector similarity search

# 3. Generate embeddings- **Search Engine**: [Meilisearch](https://www.meilisearch.com/) - Fast search API

railway run bun run generate-embeddings- **AI/ML**: [Transformers.js](https://huggingface.co/docs/transformers.js) - Run transformers in Node

- **Embedding Model**: `Xenova/all-MiniLM-L6-v2` (384 dimensions)

# 4. Sync to Meilisearch

railway run bun run sync-meilisearch### Infrastructure & Tools

```

- **Package Manager**: Bun - All-in-one toolkit

### Deploy Frontend- **Development**: Nodemon - Auto-reload on changes

- **API Testing**: cURL, Postman compatible

#### Vercel (Recommended)- **Database GUI**: Prisma Studio - Built-in database browser

- **Rate Limiting**: express-rate-limit (1000 req/15min)

1. Install Vercel CLI: `npm i -g vercel`- **CORS**: Configured for localhost development

2. Run in project root:- **Caching**: React Query (2-minute stale time)

   ```bash

   vercel### Search Technology

   ```

3. Follow prompts- **Keyword Search**: Meilisearch with typo tolerance

4. Add environment variable:- **Semantic Search**: Cosine similarity with pgvector

   ```- **Hybrid Search**: 60% semantic + 40% keyword (weighted combination)

   VITE_API_URL=https://your-backend.railway.app- **Vector Dimensions**: 384 (MiniLM-L6-v2 model)

   ```- **Similarity Metric**: Cosine distance (`<=>` operator)

5. Deploy: `vercel --prod`

---

#### Netlify

## 🚀 Deployment

1. Create account at [netlify.com](https://www.netlify.com/)

2. Click "Add new site" → "Import from Git"### Prerequisites for Production

3. Select repository

4. Configure:- PostgreSQL database with pgvector extension

   ```- Meilisearch Cloud or self-hosted instance

   Build command: bun run build- Node.js 18+ environment

   Publish directory: dist

   ```### Deploy Backend API

5. Add environment variable:

   ```#### Option 1: Railway (Recommended)

   VITE_API_URL=https://your-backend.railway.app

   ```1. Create account at [railway.app](https://railway.app/)

6. Deploy!2. Click "New Project" → "Deploy from GitHub"

3. Select your repository

### Meilisearch Cloud4. Configure:

   ```

1. Create account at [meilisearch.com](https://www.meilisearch.com/cloud)   Root Directory: server

2. Create a new project   Build Command: bun install && bun prisma generate

3. Copy API URL and key   Start Command: bun run start

4. Update backend `.env`:   ```

   ```env5. Add environment variables (all from `.env`)

   MEILISEARCH_HOST=https://your-project.meilisearch.io6. Deploy!

   MEILISEARCH_API_KEY=your-api-key

   ```#### Option 2: Render

5. Run sync: `bun run sync-meilisearch`

1. Create account at [render.com](https://render.com/)

---2. Click "New" → "Web Service"

3. Connect GitHub repository

## 📖 Additional Documentation4. Configure:

   ```

- **Meilisearch Setup**: [MEILISEARCH-SETUP.md](./MEILISEARCH-SETUP.md)   Root Directory: server

- **Database Schema**: [server/prisma/schema.prisma](./server/prisma/schema.prisma)   Build Command: bun install && bun prisma generate && bun prisma migrate deploy

- **API Routes**: [server/src/routes/](./server/src/routes/)   Start Command: bun run start

- **Seed Data**: [server/prisma/seed.ts](./server/prisma/seed.ts)   ```

5. Add environment variables

---6. Deploy!



## 🤝 Contributing#### Post-Deployment Steps



We welcome contributions! Here's how:```bash

# 1. Run migrations

1. **Fork** the repositoryrailway run bun prisma migrate deploy  # Railway

2. **Create** a feature branch:# or

   ```bashrender run bun prisma migrate deploy   # Render

   git checkout -b feature/amazing-feature

   ```# 2. Seed database

3. **Commit** your changes:railway run bun prisma db seed

   ```bash

   git commit -m 'Add amazing feature'# 3. Generate embeddings

   ```railway run bun run generate-embeddings

4. **Push** to branch:

   ```bash# 4. Sync to Meilisearch

   git push origin feature/amazing-featurerailway run bun run sync-meilisearch

   ``````

5. **Open** a Pull Request

### Deploy Frontend

### Development Guidelines

#### Vercel (Recommended)

- Write TypeScript with proper types

- Follow existing code style1. Install Vercel CLI: `npm i -g vercel`

- Add comments for complex logic2. Run in project root:

- Test your changes locally   ```bash

- Update documentation if needed   vercel

   ```

---3. Follow prompts

4. Add environment variable:

## 📝 License   ```

   VITE_API_URL=https://your-backend.railway.app

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.   ```

5. Deploy: `vercel --prod`

---

#### Netlify

## 🙏 Acknowledgments

1. Create account at [netlify.com](https://www.netlify.com/)

- **UI Components**: [Shadcn UI](https://ui.shadcn.com/) - Beautiful component library2. Click "Add new site" → "Import from Git"

- **AI Model**: [all-MiniLM-L6-v2](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2) - Sentence transformers3. Select repository

- **Search Engine**: [Meilisearch](https://www.meilisearch.com/) - Fast search API4. Configure:

- **Icons**: [Lucide](https://lucide.dev/) - Beautiful & consistent icons   ```

- **Database**: [Supabase](https://supabase.com/) - PostgreSQL with pgvector   Build command: bun run build

- **Inspiration**: npm, PyPI, crates.io package registries   Publish directory: dist

   ```

---5. Add environment variable:

   ```

## 📬 Contact & Support   VITE_API_URL=https://your-backend.railway.app

   ```

- **GitHub**: [@syedtaj7](https://github.com/syedtaj7)6. Deploy!

- **Repository**: [PackHunt](https://github.com/syedtaj7/Packhunt)

- **Issues**: [Report a bug](https://github.com/syedtaj7/Packhunt/issues)### Meilisearch Cloud



---1. Create account at [meilisearch.com](https://www.meilisearch.com/cloud)

2. Create a new project

<div align="center">3. Copy API URL and key

4. Update backend `.env`:

### 🎯 Project Checklist - "Is My Project Running?"   ```env

   MEILISEARCH_HOST=https://your-project.meilisearch.io

Use this checklist to verify everything is working:   MEILISEARCH_API_KEY=your-api-key

   ```

- [ ] ✅ Backend server running on http://localhost:30015. Run sync: `bun run sync-meilisearch`

- [ ] ✅ Meilisearch running on http://localhost:7700

- [ ] ✅ Frontend running on http://localhost:8080---

- [ ] ✅ Database seeded with 30 packages

- [ ] ✅ Embeddings generated for all packages## 📖 Additional Documentation

- [ ] ✅ Meilisearch synced with package data

- [ ] ✅ Health check returns `{"status":"ok"}`- **Meilisearch Setup**: [MEILISEARCH-SETUP.md](./MEILISEARCH-SETUP.md)

- [ ] ✅ Search works in all three modes- **Database Schema**: [server/prisma/schema.prisma](./server/prisma/schema.prisma)

- [ ] ✅ Packages display correctly- **API Routes**: [server/src/routes/](./server/src/routes/)

- [ ] ✅ Filters work (language, stars, license)- **Seed Data**: [server/prisma/seed.ts](./server/prisma/seed.ts)



**All checked?** 🎉 **You're ready to go!**---



---## 🤝 Contributing



**Made with ❤️ by Syed Taj**We welcome contributions! Here's how:



⭐ **Star this repo** if you find it helpful!1. **Fork** the repository

2. **Create** a feature branch:

[Report Bug](https://github.com/syedtaj7/Packhunt/issues) • [Request Feature](https://github.com/syedtaj7/Packhunt/issues)   ```bash

   git checkout -b feature/amazing-feature

</div>   ```

3. **Commit** your changes:
   ```bash
   git commit -m 'Add amazing feature'
   ```
4. **Push** to branch:
   ```bash
   git push origin feature/amazing-feature
   ```
5. **Open** a Pull Request

### Development Guidelines

- Write TypeScript with proper types
- Follow existing code style
- Add comments for complex logic
- Test your changes locally
- Update documentation if needed

---

## 📝 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

## 🙏 Acknowledgments

- **UI Components**: [Shadcn UI](https://ui.shadcn.com/) - Beautiful component library
- **AI Model**: [all-MiniLM-L6-v2](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2) - Sentence transformers
- **Search Engine**: [Meilisearch](https://www.meilisearch.com/) - Fast search API
- **Icons**: [Lucide](https://lucide.dev/) - Beautiful & consistent icons
- **Database**: [Supabase](https://supabase.com/) - PostgreSQL with pgvector
- **Inspiration**: npm, PyPI, crates.io package registries

---

## 📬 Contact & Support

- **GitHub**: [@syedtaj7](https://github.com/syedtaj7)
- **Repository**: [PackHunt](https://github.com/syedtaj7/Packhunt)
- **Issues**: [Report a bug](https://github.com/syedtaj7/Packhunt/issues)

---

<div align="center">

### 🎯 Project Checklist - "Is My Project Running?"

Use this checklist to verify everything is working:

- [ ] ✅ Backend server running on http://localhost:3001
- [ ] ✅ Meilisearch running on http://localhost:7700
- [ ] ✅ Frontend running on http://localhost:8080
- [ ] ✅ Database seeded with 30 packages
- [ ] ✅ Embeddings generated for all packages
- [ ] ✅ Meilisearch synced with package data
- [ ] ✅ Health check returns `{"status":"ok"}`
- [ ] ✅ Search works in all three modes
- [ ] ✅ Packages display correctly
- [ ] ✅ Filters work (language, stars, license)

**All checked?** 🎉 **You're ready to go!**

---

**Made with ❤️ by Syed Taj**

⭐ **Star this repo** if you find it helpful!

[Report Bug](https://github.com/syedtaj7/Packhunt/issues) • [Request Feature](https://github.com/syedtaj7/Packhunt/issues)

</div>
